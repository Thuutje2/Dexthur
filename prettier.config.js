module.exports = {
  semi: true,
  singleQuote: true,
  tabWidth: 2,
  trailingComma: 'es5',
};
// This configuration file sets up Prettier with the following options:
// - `semi`: true - This means that semicolons will be added at the end of statements.
// - `singleQuote`: true - This means that single quotes will be used instead of double quotes for strings.
// - `tabWidth`: 2 - This sets the number of spaces per indentation level to 2.
// - `trailingComma`: "es5" - This means that trailing commas will be added where valid in ES5 (objects, arrays, etc.).
//
